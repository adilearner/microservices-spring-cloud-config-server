server:
  port: 8085
spring:
  application:
    name: CONFIG-SERVER
  
  cloud:
    config:
       server:
         git:
           uri: https://github.com/adilearner/microservices-configuration-server
           clone-on-start: true
        
            
  
  

